mock_req <- function() {
  req <- list(
    body =
      list(
        config = list(
          resolution = 0.5
        ),
        type = "louvain"
      )
  )
}

mock_scdata <- function() {
  asd
}

test_that("louvain clustering works", {

})

test_that("leiden clustering works", {

})
