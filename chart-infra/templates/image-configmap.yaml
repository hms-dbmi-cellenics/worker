apiVersion: v1
kind: ConfigMap
metadata:
  name: instance-images
  namespace: {{.Release.Namespace}}
  labels:
    sandboxId: "{{ .Values.sandboxId }}"
data:
  r: "{{ .Values.r.image }}"
  python: "{{ .Values.python.image }}"